= form_with url: search_path, method: :get, local: true do |f|
  = f.text_field :query, placeholder: 'Search'
  = f.select :resource, options_for_select(SearchService::RESOURCES)
  = f.submit 'Search', class: 'btn btn-outline-success'

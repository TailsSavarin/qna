$('#comment-<%= @comment.id %>-errors').html('<%= render 'shared/errors', resource: @comment %>');

<% if @comment.persisted? %>
  $('.comments').append('<%= j render @comment %>');
  $('.new-comment')[0].reset();
  $('form.new-comment').hide();
  $('.add-comment').show();
<% end %>



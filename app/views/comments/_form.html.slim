div id="new-comment-#{commentable.class.name.downcase}-#{commentable.id}-errors"

= form_with model: [commentable, local], class: 'new-comment hidden', html: { id: "new-comment-#{commentable.class.name.downcase}-#{commentable.id}" } do |f|
  = f.text_area :body, class: 'form-control', placeholder: 'Enter your comment here...'
  = f.submit 'Post Your Comment', class: 'btn btn-outline-secondary btn-sm btn-block mb-2 mt-2'
